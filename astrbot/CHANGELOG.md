# 更新日志

## 1.0.2

- 触发方式改为 `EventMessageType.ALL` 全局拦截（同千小妹插件）
- 不再依赖 `wake_prefix`：`/luck`、`luck`、`今日运势`、`#luck`、`小爱 luck` 均可直接触发
- 命中后 `stop_event()`，不进入后续 LLM

## 1.0.1

- 新增指令 `/更新luck`（别名 `/更新运势` / `luck更新`），从 GitHub 更新代码，保留配置
- 字体查找跨平台（Windows / Linux / macOS）
- 类名改为 `WuwaLuckPlugin`，补 `@register`，发消息改为 `await event.send(...)`
- 宜/忌大字加粗、小字微调；水印遮盖；谐振指数：限定池 / 武器池 / 声骸调谐

## 1.0.0

- 白底运势卡：吉凶、谐振指数、宜/忌、共鸣指引
- 宜/忌默认各 2 条；大吉「诸事皆宜」、大凶「诸事不宜」特判
- 鸣潮文案补充，全卡最多 1 条
- 共鸣指引：小爱（爱弥斯）约 80%；主角色语录 5 句
- WebUI：各角色 0–100 权重、宜忌鸣潮概率、谐振指数条目
